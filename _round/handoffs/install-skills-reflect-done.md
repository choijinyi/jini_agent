# handoff — install-skills-reflect (done)

> ⚠ `HANDOFF_CONTRACT` 의 「5필드」는 **정의를 찾지 못했다.** `javis_task.py` 1562행의 리마인더
> 문자열이 유일한 언급이고, `directives/`·`bin/` 어디에도 필드 스키마가 없다. 기존 실례도 없다
> (`_round/handoffs/` 자체가 이 파일로 처음 생겼다). 그래서 아래 5필드는 **내가 정한 것**이고
> 계약 준수를 주장하지 않는다 — 정본 스키마가 나오면 이 파일을 그 형식으로 다시 써야 한다.

## 1. 무엇을 했나 (what)

오너 직접 지시 「스킬 넣은 것을 깃허브 설치 파일에도 반영」의 잔여분. 커밋 `ac02bbe`.

- 설치 마지막에 **스킬 개수 고지 + 잔여 위험 4줄** 신설 (`src/tools/skills-verify.js`)
- 스킬 확인 단계를 `install.sh`·`install.ps1` **파일 마지막으로 이동**
- 정합이 깨졌을 때 개수를 단정하지 않도록 머리말 분기
- `package.json` `files` 에 `skills` 추가
- README 설치 절차 절에 스킬 단계·개수·무네트워크 명시
- selftest 회귀 5건 추가

## 2. 왜 (why)

착수 시점 실측으로 지시 5항 중 **3항(사용자 고지 + 잔여 위험)만 미이행**이었다.
1·2항은 선행 커밋 `b24aea5`·`09687db` 로 이미 완료였고, 4항은 스킬 체계 절만 있고
설치 절차 절에 누락, 5항은 부분(2건)이었다. 고지가 없으면 사용자가 제3자 코드 실행
가능성을 모르고 설치한다 — 「조항은 있고 기전은 없는 상태」의 반대 형태다.

## 3. 현재 상태 (state)

- HEAD `e5b30cd` 시점 기준. `npm run selftest` = **87개 통과 exit 0**(착수 시 82).
- 실데이터 정합 검사 6종 전부 ok · 120개 · 고정 커밋 `06d017ac0531`.
- **스킬 콘텐츠 0바이트 변경** · 보류 3종 미부활 · 네트워크 재다운로드 0.
- master 가 레인 완료 승인. worker(surface:56) 독립 검증이 최종 게이트로 남아 있다.

## 4. 남는 위험 (risk)

- 이 스킬들은 실행 시 **제3자 파이썬 코드를 돌릴 수 있다.** 고정·선별·기록했을 뿐 능력을 없애지 않았다.
- **실측된 결함 2건**(미측정이 아니다): 설명에 한글이 하나도 없는 스킬 **21/120** ·
  부정문 때문에 반대 의미로 걸리는 스킬 2건. 근거 `_round/evidence/final-verification.md` §4-B.
- 재지 못한 것: 두 설치 스크립트 전 구간 실행(electron 다운로드 비용) · Linux/macOS 실기.
- ⚠ 21/120 을 즉석 정규식으로 재면 **23** 이 나온다(YAML 블록 스칼라 2건 오탐).
  정본은 저장소 프런트매터 파서 기준 **21**.

## 5. 다음 (next)

- worker(surface:56) 독립 검증 결과 수령 → 반증이 오면 그것이 우선한다.
  ⚠ 검증 기준 커밋 재지정 필요: master 가 재현한 `81ff99f`(selftest 82)는 현재 HEAD 보다 3커밋 뒤다.
- 다음 레인 후보(**오너 판단 대기 · 착수 금지**): 21개에 한국어 트리거 보강 여부.
  상류 원문 변경이므로 `PROVENANCE.json` `modifications` 고지 + 갱신 3단 절차 대상이다.
- api 경로 실측은 키가 생기면 재개. 절차는 `final-verification.md` §4-2 에 4단으로 남겨 뒀다.
